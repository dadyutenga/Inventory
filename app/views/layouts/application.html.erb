<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Ditronics Inventory" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Inventory">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%# Font Awesome for crisp icons %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" crossorigin="anonymous" />
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <div class="app-shell">
        <%= render "shared/sidebar" %>

        <main class="app-main">
          <header class="app-topbar">
            <div>
              <p class="app-kicker">Welcome back, <%= current_user.name || current_user.email %></p>
              <h1 class="app-title"><%= content_for?(:page_heading) ? yield(:page_heading) : (content_for?(:title) || "Dashboard") %></h1>
            </div>
            <% if content_for?(:topbar_actions) %>
              <div class="app-actions"><%= yield :topbar_actions %></div>
            <% end %>
          </header>

          <% if notice.present? %>
            <div class="flash flash--notice"><%= notice %></div>
          <% end %>

          <% if alert.present? %>
            <div class="flash flash--alert"><%= alert %></div>
          <% end %>

          <div class="app-content">
            <%= yield %>
          </div>
        </main>
      </div>
    <% else %>
      <%= yield %>
    <% end %>

    <style>
      :root {
        --accent: #2d8a75;
        --bg: #f3f4f6;
        --card: #ffffff;
        --border: #e5e7eb;
        --text: #0f172a;
        --muted: #6b7280;
      }
      * { margin: 0; padding: 0; box-sizing: border-box; }
      body { font-family: 'IBM Plex Sans', 'Segoe UI', system-ui, sans-serif; background: var(--bg); color: var(--text); }

      .app-shell { display: grid; grid-template-columns: 260px 1fr; min-height: 100vh; background: var(--bg); }
      .app-main { padding: 24px 32px; }
      .app-topbar { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
      .app-kicker { font-size: 13px; color: #6b7280; letter-spacing: 0.02em; margin-bottom: 4px; }
      .app-title { font-size: 26px; font-weight: 700; color: #0f172a; }
      .app-actions { display: flex; gap: 10px; }
      .app-content { display: block; }

      .sidebar { background: #2d8a75; color: #ffffff; padding: 24px 20px; display: flex; flex-direction: column; gap: 16px; min-width: 220px; }
      .sidebar__brand { margin-bottom: 8px; }
      .sidebar__brand-link { text-decoration: none; display: block; }
      .sidebar__brand-title { font-weight: 800; font-size: 16px; color: #ffffff; }
      .sidebar__brand-subtitle { font-size: 12px; color: rgba(255,255,255,0.85); margin-top: 2px; }
      .sidebar__nav { display: flex; flex-direction: column; gap: 6px; }
      .sidebar__nav-link { display: flex; align-items: center; gap: 12px; padding: 11px 14px; border-radius: 8px; color: #ffffff; text-decoration: none; transition: background 0.15s ease, transform 0.15s ease; font-size: 14px; font-weight: 600; }
      .sidebar__nav-link:hover { background: rgba(0,0,0,0.12); color: #ffffff; transform: translateX(2px); }
      .sidebar__nav-link.is-active { background: rgba(0,0,0,0.22); color: #ffffff; }
      .sidebar__nav-link:hover { background: rgba(0,0,0,0.10); color: #ffffff; transform: translateX(1px); }
      .sidebar__icon { width: 18px; text-align: center; font-size: 15px; }
      .sidebar__actions .logout-btn { display: flex !important; align-items: center; gap: 12px; padding: 11px 14px; border-radius: 8px; color: #ffffff; text-decoration: none; transition: background 0.15s ease, transform 0.15s ease; font-size: 14px; font-weight: 600; cursor: pointer; }
      .sidebar__actions .logout-btn:hover { background: rgba(0,0,0,0.12); color: #ffffff; transform: translateX(2px); }
      .sidebar__footer { margin-top: auto; padding-top: 12px; border-top: 1px solid rgba(255,255,255,0.25); }
      .sidebar__settings { display: flex; align-items: center; gap: 12px; padding: 11px 14px; color: #ffffff; cursor: pointer; font-size: 14px; font-weight: 600; border-radius: 8px; }

      .flash { padding: 14px 16px; border-radius: 10px; margin-bottom: 16px; font-weight: 600; }
      .flash--notice { background: #ecfdf3; color: #15803d; border: 1px solid #bbf7d0; }
      .flash--alert { background: #fef2f2; color: #b91c1c; border: 1px solid #fecdd3; }

      .btn { padding: 9px 14px; border-radius: 6px; border: 1px solid var(--border); cursor: pointer; font-weight: 600; text-decoration: none; display: inline-flex; align-items: center; gap: 6px; font-size: 14px; background: transparent; color: var(--text); transition: background-color 0.12s ease, border-color 0.12s ease, color 0.12s ease; }
      .btn-primary,
      .btn-secondary,
      .btn-muted { background: transparent; color: var(--accent); border-color: rgba(45,138,117,0.35); }
      .btn-primary:hover,
      .btn-secondary:hover,
      .btn-muted:hover { background: rgba(45,138,117,0.08); border-color: rgba(45,138,117,0.45); color: var(--accent); }
      .btn-sm { padding: 7px 10px; font-size: 12px; }

      @media (max-width: 960px) {
        .app-shell { grid-template-columns: 1fr; }
        .sidebar { position: sticky; top: 0; z-index: 10; border-bottom: 1px solid rgba(255,255,255,0.08); }
      }
    </style>
  </body>
</html>
